buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      agent:
        image: golang
      setup:
        steps:
        - groovy: checkout scm
      build:
        steps:
        - sh: /usr/local/go/bin/go get github.com/golang/dep
          name: go-get-dep
        - sh: make build
          name: make-linux
        - sh: make test
          name: make-test